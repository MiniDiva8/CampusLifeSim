# 构建与验证报告

日期：2026-07-26
版本：0.8.0-demo

## 环境

- Godot：4.7.1.stable.official.a13da4feb 标准版
- 渲染：Compatibility / OpenGL 3.3
- 目标：Windows Desktop x86_64 debug
- 导出模板：4.7.1.stable Windows debug/release 已安装

## 自动化结果

- 核心规则、难度、存档、图片清单、场景元数据与音频配置测试：78 项通过。
- 独立音频冒烟测试：9 项通过，覆盖语义按钮音、开关状态音、直接事件提示、分组静音、压力音效开关、总线淡入淡出和退出清理。
- 独立校园声景冒烟测试：9 项通过，覆盖九类声音上下文、白天/傍晚/深夜映射、道路交叉淡化、目的地预热、高压身体反馈和恢复淡出。
- UI 流程冒烟测试：45 项通过，覆盖三个难度、难度存档、压力过载、真实按钮回调、四层用户音量设置、压力音效开关、道路与地点声景、固定事件语义照片、自适应竖图宽度、照片/面板分离、原始纹理尺寸与 EXIF 显示方向，以及玻璃面板、按钮悬停控制器、真实抬升缩放、地点卡、发光滑杆和道路进度条。
- 完整七天模拟：简易难度的平衡、学习、AI 三条路线，以及中等、困难的平衡路线，均在 33 个行动后到达项目展示并产生结局。
- 0.8.0 使用 Windows OpenGL 3.3 和 NVIDIA GeForce RTX 5060 Laptop GPU 重新截取 1280×720 视觉基线；Neo Glass 着色器在 Compatibility 渲染器下正常编译，主菜单、地图、事件、结算、设置、暂停、道路、压力危机和结局均没有重叠或溢出。
- 玻璃层检查：主信息面板能够读取并柔化背后画面，文字和控件保持清晰；同屏内部卡片使用轻量半透明样式，避免多重屏幕取样。校园照片舞台不经过玻璃着色器。
- 交互检查：地图地点卡通过自动测试真实触发鼠标进入信号，在 0.18 秒后产生大于 1.0 的抬升缩放；按钮同时保留键盘焦点样式与“减少界面动效”降级路径。
- 自适应照片检查：网球与理综楼走廊使用 474 像素竖幅舞台，常规横图与宽图分别使用 652 或 704 像素舞台；照片完整等比显示，交互面板不与照片重叠。
- 路途检查：EXIF 旋转的竖幅道路照片在等比放大和平移过程中保持完整画面，不再因 offset 动画被挤成细条。
- 图片检查：65 张项目副本与用户原图逐张 SHA-256 一致，总计 313.16 MiB；未缩放、未重新编码、未二次压缩。
- 场景检查：中心校区六个地点、操场八类照片、教学区具名空间和固定日程事件均能显示对应真实名称；事件标题、正文、选项和结果支持当前场景替换。
- 内容检查：32 个事件数量和效果结构保持不变，并已加入人工智能专业语境、数据集划分、随机种子、模型评测与幻觉核验等叙事。
- 音频检查：七个总线与 Master 限制器正常加载，14 类交互音和九类自适应校园声景完成注册；快速重复触发受冷却限制，同时播放上限为八声部。
- 项目运行：Godot 4.7.1 无界面运行无脚本错误；核心、音频、声景和详细 UI 测试均正常退出。

## Windows 构建

- `CampusLifeSim.exe`、`CampusLifeSim.console.exe` 和 `CampusLifeSim.pck` 已成功生成。
- 文件大小：主程序 98.21 MiB，PCK 672.92 MiB，控制台包装器 0.05 MiB。PCK 体积主要来自 65 张原始分辨率照片的 Godot 导入纹理；Neo Glass 代码和程序化音频没有增加外部素材文件。
- EXE 元数据：产品名“惊魂期末周”，版本 `0.8.0.0`，文件说明“山东大学中心校区期末周选择叙事游戏”。
- 导出 PCK 使用 Godot 4.7.1 `--disable-crash-handler --headless --main-pack` 成功离线加载并运行 120 帧，退出码为 0，日志中没有脚本或资源错误。强制帧数退出跳过游戏自身的主动清理阶段，因此该命令报告两个 `ObjectDB` 实例退出时残留；独立音频、声景和 UI 测试的主动清理路径均正常退出。
- SHA-256：`CampusLifeSim.exe` 为 `9425CDF6DFB64BEEB097963ED61FD8845863CC2621330CF4B736B26687545DC1`；`CampusLifeSim.pck` 为 `BC07857B04D498CEB605ACEB6A23B3AFBA24BFDEDF7C293BD3121E7992ECC690`。
- 0.5.0 直接启动 EXE 时曾被当前电脑的 Windows App Control for Business 拦截。为避免再次制造安全弹窗，本次没有直接运行未签名的 0.8.0 EXE；PCK 已通过受信任的官方 Godot 4.7.1 完成离线加载验证。
- 已验证项目根目录的 `启动游戏（安全方式）.cmd` 能调用本机官方签名 Godot 4.7.1，通过 `--main-pack` 加载同一 PCK。图形进程成功运行并生成日志，日志中没有脚本或资源错误。
- 按项目约束未创建安装程序、未进行代码签名，也未修改系统安全策略。

当前开发电脑请使用项目根目录的安全启动器。正式对外分发时，应使用受比赛设备策略信任的证书签署 release 构建，或由比赛主办方/设备管理员通过补充策略允许该构建，不要通过关闭安全功能绕过。
