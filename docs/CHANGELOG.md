# 变更记录

## 0.11.2-transit-quiet

- 路线登记页将姓名改为独立的“档案署名”字段，缩短开场提问并调整三条路线页签位置，避免标题、提示和姓名输入相互挤压。
- 地图删除与原有黑色半透明提示重叠的白色活动浮签；地点悬停恢复只显示原有地点说明，避免同一建筑出现两套文字。
- 地图和行程页不再向玩家显示“约 2 秒”或“预计 2 秒”等机械倒计时文案；实际道路过渡时长保持不变。
- 道路转场进度条新增原创背书包学生行走插画，角色随进度从起点走向目的地。
- 从全部校园声景移除夜间高频脉冲合成声，保留低频环境底噪、风声、脚步和地点语义声，不再持续产生类似警报的“嘀嘀”声。
- UI 回归扩展至 80 项，脚本扫描、声景冒烟测试和真实 GPU 视觉截图均通过。

## 0.11.1-receipt-hover

- 将遗漏的深色选择结果面板迁移为“校园纪实选择回执”，继续使用事件发生时的原比例照片与暖纸排版。
- 回执明确区分“本时段已经写入档案”“结果已结算”和“翻页 · 继续期末周”，数值变化作为回执正文展示，不再回到黑色玻璃卡片。
- 六个地图建筑新增即时纸质活动浮签；鼠标悬停或键盘聚焦时显示真实地点名和两项可进行行动，移开后立即收起。
- 悬停浮签使用地点强调色、建筑连线和不拦截鼠标的独立层，点击后原有路线绘制、底部详情和两秒路途保持不变。
- UI 回归扩展至 78 项，并新增地图悬停视觉基线；选择回执、照片纹理复用与完整地图流程均通过。

## 0.11.0-archive-routes

- 主菜单从暗色学校宣传页改为“等待启封的期末周档案封面”，使用暖纸、折痕、目录索引、日期戳、图钉、地点圈注与保持 4:3 原比例的中心校区档案附图。
- “开始新的一周”改为具有仪式感的“启封档案 · 开始第 1 天”；新建、继续、设置、许可与退出以档案目录条目呈现，不再排列成五个后台式大按钮。
- 角色初始化改为双页“七天策略路线档案”：左页使用错层文件页签选择稳扎稳打、实干派或协调者，右页同步解释核心风格、初始优势、潜在短板、推荐行动和事件倾向。
- 难度选择从三张大卡片收束为简易、中等、困难三节点强度轴；姓名改为档案记录人签字线，保留鼠标、键盘焦点和减少界面动效支持。
- 三条路线加入真实规则：稳扎稳打提高学习与考试收益、实干派提高项目收益并额外积累行动压力、协调者提高关系收益；路线还会调整学业、项目或 NPC 事件优先级。
- 选项结果预览同步显示难度和路线共同调整后的真实数值，不在界面中承诺未实现的玩法效果。
- 核心测试扩展至 97 项，UI 流程扩展至 73 项；完整七天模拟、真实图形截图和 Godot 4.7.1 脚本扫描均通过。

## 0.10.0-campus-map

- 校园总览从“三栏仪表盘 + 2×3 等宽地点卡”重做为全屏纸质校园导览图，不再使用纯黑背景、深色大面板和六张功能卡。
- 六个地点分别绘制学生公寓楼群、蒋震图书馆柱廊、教学楼群、AI 学院机房、齐园餐厅和风雨操场轮廓，建筑本身就是可聚焦、可点击的热点。
- 玩家点选建筑后，地图会从当前地点或中心广场绘制山大红路线，并只在底部展开当前地点的名称、活动、描述和单一“前往这里”操作。
- 四名 NPC 以人物落点固定在相关地点，鼠标提示显示角色身份与关系状态；考试和项目展示改为地图边缘的纸质截止日便签。
- 顶部只保留姓名、日期、难度与四项核心数值，暂停和 AI 建议退到页边操作，避免再次形成后台式左右侧栏。
- 地点选择仍调用原有自动存档、照片预读、约两秒道路过渡和事件结算，不增加自由行走或寻路。
- UI 回归新增校园地图画布、无旧玻璃面板、建筑选择、路线状态、真实地点详情与唯一行程按钮检查；69 项 UI、82 项核心、完整七天模拟及音频回归全部通过。

## 0.9.2-polish

- 移除玩家界面中的 `SCENE LIVE`、`横幅画报`、`竖幅原图`和`完整比例`等开发阶段版式标记。
- 地点照片仍保留真实地点和活动名称；事件页继续通过后台元数据选择构图，但不向玩家解释实现方式。
- 删除菜单每八秒重复的程序化和弦，以及道路铃声、教学楼铃声、实验室状态提示音、操场合成哨声和键盘纯音。
- 食堂的高频纯音餐具声改为低响度非音高噪声瞬态；点击、确认和事件选择等由玩家操作触发的交互音效不变。
- 环境声回归新增“禁止周期性铃声、提示音和合成哨声”契约，视觉回归新增无开发标记的地点照片截图。

## 0.9.1-photo-layout

- 只读盘点全部 65 张运行时照片：44 张显示为竖幅、21 张为 4:3 或近方形，当前没有达到 1.5 宽高比的电影横幅。
- 事件页新增电影横幅、横幅画报和竖幅跨页三种版式；使用旋转后的真实宽高比自动选择，也允许照片元数据明确指定左右位置。
- 知新楼、网球、理综楼、理综楼走廊、学校咖啡馆和压力长曝光照片加入代表性构图元数据。
- 竖幅运动、竖幅建筑和压力照片左右交替展示；4:3 教学空间使用 640×480 原比例画报板，不伪装成 16:9 全屏。
- 压力危机迁移到同一校园纪实事件排版，保留长曝光原图、难度效果与身体反馈动画。
- 新增照片清单、后续横屏拍摄规范和导入验收清单；原始图片文件未被修改。

## 0.9.0-event-prototype

- 事件选择页从 Neo Glass 双卡片结构改为“校园纪实摄影 × 期末周编辑排版”单画布样板；主菜单、地图、设置等页面本轮不批量迁移。
- 用户原始校园照片完整居中显示并保持原比例，画布余白改用照片语境中的石材灰白；不再使用纯黑背景、深色大面板或照片相框。
- 顶部 HUD 改为页边日期、真实场景名和三项状态排版，取消状态胶囊与边框容器。
- 事件正文使用固定宽度中文段落排版，选项改为无边框编号列表，结果预览直接作为行尾文字显示。
- 鼠标悬停和键盘聚焦只触发山大红页边标、文字位移与底部细线延长，不抬升整块选项。
- 点击结算继续沿用即时锁定和单帧反馈，UI 冒烟实测事件点击反馈为 0.33ms。
- 新增事件页视觉方向契约、三张构图探针、项目级设计 Skill 来源记录和独立 `PRODUCT.md`。
- 核心 78 项、UI 58 项、交互音效 9 项、环境声 9 项及完整七天模拟全部通过。

## 0.8.1-demo

- 修复事件选项、继续按钮和地点入口在首次遇到超高分辨率照片时偶发的长帧卡顿。
- 同一照片纹理现在只读取一次，并直接传给比例判断和照片舞台，不再由三个界面步骤分别请求。
- 新增最多四张的 LRU 原图缓存，复用当前事件、结果、道路和目的地纹理，同时避免一次性预载 65 张原图造成内存压力。
- 道路、目的地、下一幕固定事件和压力危机照片改用 Godot 后台线程准备；地点点击会立即进入确认页，再显示约两秒道路过程。
- 选项点击、行动结算、继续推进和压力应对会先锁定重复输入、显示忙碌状态并让出一帧，再执行结算或场景重建。
- 明确保留数据驱动的延迟后果：它只决定后果在哪个未来时段出现，不产生真实等待或阻塞。
- UI 性能回归增加点击反馈时限、纹理对象复用、后台道路准备和四张缓存上限检查。

## 0.8.0-demo

- 全面升级为“山大夜航 · Neo Glass”竞赛视觉体系，统一主菜单、角色初始化、校园地图、事件、结算、设置、暂停、路途和结局。
- 新增项目原创的分辨率无关屏幕取样毛玻璃着色器，包含受控模糊、色调融合、饱和度、细微颗粒、边缘高光和柔和投影。
- 新增共享 `GlassPanel`，把毛玻璃背景与清晰文字内容分层；照片舞台保持独立，不对原始校园照片做模糊、拉伸、重编码或压缩。
- 按钮和地点卡新增鼠标跟随聚光、悬停抬升、按压缩放、键盘焦点高亮与柔和阴影；“减少界面动效”会关闭位移动画。
- 地点入口改为更清晰的信息层级，直接展示真实地点、活动描述、进入动作和时段消耗。
- 设置滑杆、状态条与道路进度条使用统一的半透明轨道、发光填充和圆形高光抓手。
- 校园总览背景新增程序化暗色渐变、山大红/青绿/蓝色柔光和细网格，不增加外部图片依赖。
- UI 冒烟测试新增玻璃面板、悬停控制器、真实悬停缩放、地点卡、滑杆与道路进度样式检查。

## 0.7.0-demo

- 新增原创程序化校园声景，覆盖主菜单、校园总览、道路、学生公寓、蒋震图书馆、教学区、人工智能学院机房、齐园餐厅和风雨操场。
- 五个游戏时段映射为白天、傍晚和深夜三个声音状态，人群活动、脚步、风声和夜间虫声会随时间改变。
- 两秒道路过渡会从原地点交叉淡化到道路声，抵达时再交叉淡化到目的地，并在路途中预热目标循环。
- 六个地点分别加入可辨识的声音细节：风扇与键盘、翻页、楼道脚步、设备底噪、餐具碰撞、球声和远处哨声。
- 压力达到 60 后叠加独立低频身体反馈，强度随压力变化，恢复后平滑消失，并受既有“压力状态音效”开关控制。
- 所有新增声音均为固定参数与固定种子生成，没有下载第三方音频，也不依赖网络。
- 新增独立声景冒烟测试，并通过核心规则、点击音效、环境音、UI 流程、完整七天模拟、视觉捕获和 Windows 导出验证。

## 0.6.0-demo

- 用项目自有 `AudioDirector` 替换统一的 620Hz 按钮提示音，加入 14 类具有不同节奏、音高和材质的原创程序化交互声音。
- 按钮通过语义元数据选择悬停、普通点击、确认、返回、开关、地点进入、事件选择和危险操作音效；同类声音加入轻微音高变化和触发冷却。
- 音频总线扩展为 Master、Music、SFX、UI、Event、Stress 和 Ambience，并在 Master 末端加入 -1 dB 安全限制器。
- 设置页面增加音乐、交互与事件、校园环境三个独立音量，以及可单独关闭的压力状态音效；旧设置文件会自动补齐新字段。
- 增加八声部并发上限、总线淡入淡出、退出前音频清理和独立音频冒烟测试，避免快速点击堆叠与退出时资源残留。
- 当前阶段没有下载或加入外部音乐与音效文件；校园环境声将在下一阶段单独建立并登记来源。

## 0.5.0-demo

- 将六个地图入口替换为山东大学中心校区真实语境：中心校区学生公寓、蒋震图书馆、中心校区教学区、人工智能学院机房、齐园餐厅和风雨操场。
- 蒋震图书馆与阅读空间照片从教学场景池归回图书馆，固定答疑、考试和展示分别绑定理综楼、董明珠楼与青年会客厅照片。
- 风雨操场、综合体育馆、游泳馆、室外田径场和室外网球场按照片分别显示，不再用一个操场名称覆盖所有运动。
- 教学、咖啡馆和青年会客厅场景提供照片匹配的行动文案；选择结果继续使用当时展示的具体行动名称。
- 32 个事件保持数量和数值结构不变，并加入数据划分、随机种子、可复现性、虚构论文、模型评价和 AI 课程答辩等专业细节。
- 主菜单、路途、场景 HUD、加载与结局加入克制的山大红、中心校区、1901及校训元素，不使用山东大学校徽。
- 新增 `启动游戏（安全方式）.cmd`：在 WDAC 阻止未签名 EXE 的开发电脑上，通过已安装的官方签名 Godot 4.7.1 加载 PCK，不关闭或绕过系统安全功能。
- 项目版本更新为 `0.5.0-demo`，学校信息来源记录在素材与来源文档中。

## 0.4.0-demo

- 将确认通过的竞赛界面原型迁移到正式流程，新增可交互的 `AdaptiveSceneView`。
- 地点、事件、结果与压力危机统一使用独立照片舞台和右侧选择面板，不再以大卡片遮盖背景。
- 根据旋转后的原图比例自动采用竖幅、标准横幅或宽幅布局，照片不拉伸、不重新编码、不二次压缩。
- 固定日程事件按答疑、考试、项目会议、彩排等语义选择教学楼、图书馆或实验室照片。
- 选择卡会显示难度调整后的主要数值影响；隐藏 AI 依赖度仍不显示具体数值。
- 主菜单、校园总览、路途、角色初始化、暂停、设置、制作信息和结局统一为深色竞赛展示视觉。
- 道路过渡改为等比放大后的轻微平移，修正 EXIF 旋转竖图在滚动时被挤压的问题。
- UI 冒烟测试扩展到 33 项，覆盖真实按钮回调、语义照片、自适应竖图宽度与照片/面板分离。

## 0.3.0-demo

- 将项目内 65 张照片替换为与用户源文件 SHA-256 一致的原图，不再缩放或二次压缩。
- 新增原比例“完整前景 + 同图柔和铺底”的宽屏显示方式，避免照片被强制铺满裁切。
- 在显示层应用 EXIF 方向元数据，修正理综楼走廊、宿舍、道路及部分食堂照片横倒的问题，同时保持原 JPEG 字节不变。
- 降低事件遮罩、卡片和选择按钮的不透明度，并适度缩小事件卡片，让照片内容更清楚。
- 操场照片会明确说明打网球、游泳、篮球、羽毛球、跑步等真实活动。
- 教学区照片会显示理综楼、理综楼走廊、知新楼、董明珠楼、青年会客厅等真实场景名称。
- 场景事件支持 `{scene_name}` 与 `{scene_activity}` 占位符，使标题、正文、选项和结果与当前照片一致。

## 0.2.0-demo

- 新增简易、中等、困难三档难度，新游戏默认推荐中等。
- 难度统一影响压力增长与缓解、精力消耗与恢复、学习/项目/考试/展示收益。
- 高压达到难度阈值后自动触发眩晕危机，并提供恢复、冒险推进和关系求助三种处理方式。
- 将用户提供的长曝光光轨照片用于压力过载背景，并加入持续的视觉拉扯动效。
- 难度、危机次数和选择结果随自动存档保存；旧存档继续采用原本的简易节奏。

## 0.1.1-demo

- 接入用户提供的 64 张校园照片，并按六个地点、昼夜道路与菜单分类。
- 地点进入时随机选择背景，同一地点不会连续显示同一张；选择结果和随机历史随存档保存。
- 加入约两秒的校园道路滚动过渡，傍晚和夜间自动使用夜景池。
- 新增照片方向校正与尺寸优化导入工具、运行时资源清单和相应测试。
- 更新 Windows debug 构建并完成 EXE/PCK 离线启动验证。

## 0.1.0-demo

- 初始化 Godot 4.7.1 独立项目与长期开发文档。
- 实现七天时间、玩家状态、NPC 关系、延迟后果和固定种子 AI。
- 加入 32 个事件、六个地点、一个考试、一次展示和七个结局。
- 完成主菜单、校园地图、地点行动、设置、暂停、自动存档和结局流程。
- 选择性接入 Maaack 场景加载、音频与暂停基础。
