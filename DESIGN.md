---
name: CampusLifeSim Editorial Event Surface
status: active-system
scope: archive-menu/route-setup/event-choice-screen/campus-map
baseline: 1280x720
concept_seed: user-pinned/no-roll
updated: 2026-07-26
---

# Overview

《惊魂期末周》的事件页采用“校园纪实照片 + 学生刊物排版”作为视觉母题。玩家先认出真实校园地点，再阅读事件，最后比较会改变状态的选择。照片是画布，不是被卡片遮住的装饰。

这套暖纸、校园纪实和档案批注语言目前已经覆盖主菜单、路线初始化、事件选择和校园地图。地点行动、结算、设置、暂停和结局页仍可继续迁移，但不得反向把已移除的纯黑背景、玻璃大卡或后台式网格带回这些核心页面。

主菜单把照片当作“档案附图”，允许使用图钉、纸胶带、日期戳、地点圈注与目录索引；路线页把三种开局方式当作可验证的策略档案，必须同时写清优势、短板、推荐行动和事件倾向。所有玩法说明都必须与实际规则一致。

选择结果页是事件页的“回执态”：继续保留同一张照片和版式，只把问题替换为已结算变化与单一翻页操作。校园地图的建筑悬停信息只保留一套黑色半透明地点提示，显示真实地点名、用途和地图操作提示，不能与第二套白色浮签叠加。

# Colors

| Token | Value | Use |
| --- | --- | --- |
| Paper | `#E9E6DC` | 页面底色与照片两侧留白 |
| Paper Light | `#F4F1E8` | 局部文字衬底、顶部轻柔洗色 |
| Graphite | `#252A2B` | 标题和关键正文 |
| Graphite Soft | `#515B5C` | 正文、选项说明 |
| Graphite Faint | `#6F7878` | 辅助状态和页脚 |
| SDU Red | `#B84850` | 时间标记、序号、当前悬停动作 |
| Mist Blue | `#527C8A` | 真实地点名称 |
| Energy | `#287C68` | 精力与正向结果 |
| Stress | `#A94F54` | 压力与风险 |
| Exam | `#9A6A18` | 考试进度 |

纸色与照片之间可以使用透明渐变，但不得对整张照片叠加统一灰雾。文字衬底必须局部化，只覆盖阅读所需区域。

# Typography

使用 Godot 默认字体和系统中文回退，不引入未登记的网络字体。字阶如下：

- 事件标题：36 px，最多三行。
- 选项标题：18 px。
- 顶部日期：17 px。
- 事件正文与地点：15 px。
- 选项说明、状态摘要：13 px。
- 成本、存档、页脚与时间轴：11–12 px。

标题使用短句和明确换行；正文保持自然行宽。不要使用全大写英文、渐变字、发光字或过量粗体。

# Layout

基准视口为 1280×720。原始照片按真实宽高比和 EXIF 方向展示，不裁切、不拉伸、不压缩源文件。

事件页根据旋转后的照片比例选择三种版式：

- `cinematic`：宽高比 ≥ 1.5。照片使用完整事件画布；左侧叙事起点 `(56, 318)`，右侧选择起点 `(676, 190)`。
- `editorial`：宽高比介于 0.85 与 1.5。照片使用 640×480 画报板，叙事和选择位于另一侧。
- `portrait`：宽高比 < 0.85。照片使用 456×540 竖幅板，并依据主体位置放在左侧或右侧。

顶部 76 px 是页眉和横向规则线，底部是当天五时段时间轴与天数。照片比例和版式名称只作为内部元数据，不得显示给玩家。排版必须适应照片，而不是为了填满屏幕破坏照片比例。

# Elevation & Depth

事件页不使用卡片阴影和悬浮面板。深度来自：

- 清晰的原始校园照片；
- 照片与暖纸底之间的材质对比；
- 局部半透明纸色渐变；
- 细规则线、编号和留白形成的层级。

不得使用纯黑遮罩、大面积毛玻璃、霓虹外发光或模拟 SaaS 仪表盘的层层卡片。

# Shapes

- 选择项默认透明、无边框、无圆角容器。
- 分隔线为 1 px，强调线为 3–4 px。
- SDU 红色竖线表示当前时间或当前悬停选择。
- 圆角只保留给确有实体语义的弹窗或输入控件，不作为通用装饰。

# Components

## Editorial Event Canvas

全屏纸色基底承载保持原比例的校园照片。电影横幅使用照片画布；竖幅和 4:3 照片使用左右交替的跨页或画报构图。顶部始终保留时间、地点和核心状态。

## Narrative Block

由章节标记、标题、正文和难度摘要构成。无容器边框，文本必须落在稳定留白或局部衬底上。

## Choice Row

由两位编号、动作标题、具体说明和预期效果组成。悬停或键盘聚焦时：

- 左侧出现 SDU 红竖线；
- 内容轻微向右移动；
- 当前分隔线变为 SDU 红；
- 反馈在 160 ms 内完成。

不得通过大面积填充矩形表现悬停。

## State Line

顶部仅展示当前决策最关键的精力、压力和考试进度。它是页边信息，不是独立卡片。

## Motion

- 叙事进入：200 ms。
- 选项进入：240 ms。
- 悬停反馈：160 ms。
- 玩家开启“减少动态效果”时，所有装饰动画立即完成。

# Do’s and Don’ts

Do:

- 让照片中的真实地点和活动名称进入标题与事件正文。
- 根据横幅或竖幅原图调整留白，而不是裁切主体。
- 用具体后果说明选项，让选择像真正的校园决策。
- 保持键盘焦点、鼠标悬停和按下反馈一致。

Don’t:

- 不恢复纯黑背景、发光玻璃卡片或整页方框网格。
- 不用通用“情境选择”代替真实地点和活动。
- 不将低字号辅助文字作为核心信息。
- 不把设计预览图当作正式运行素材。
- 不让旧页面样式污染本事件页规范。
